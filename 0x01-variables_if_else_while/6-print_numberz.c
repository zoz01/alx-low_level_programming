#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
*main - entiry point
*Return:is always 0
*/
int main(void)
{
	int mydf;

	for (mydf = '0'; mydf <= '9'; mydf++)
	putchar(mydf);
	putchar('\n');
	return (0);
}
