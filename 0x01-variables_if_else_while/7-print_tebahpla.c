#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
*main - entiry point
*Return:is always 0
*/
int main(void)
{
	char mlml;

	for (mlml = 'z'; mlml >= 'a'; mlml--)
		putchar(mlml);
		putchar('\n');
	return (0);
}
